pub struct BitmapAgent {
    rpc_url: String,
}

impl BitmapAgent {
    pub fn new(rpc_url: &str) -> Self {
        BitmapAgent {
            rpc_url: rpc_url.to_string(),
        }
    }

    pub fn execute(&self) {
        println!("Executing BitmapAgent with RPC URL: {}", self.rpc_url);
    }
}

